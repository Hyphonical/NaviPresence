---
services:
  navipresence:
    # 🖼️ Image published by GitHub Actions
    image: ghcr.io/hyphonical/navipresence:latest
    # 🐳 Container name
    container_name: NaviPresence
    restart: unless-stopped
    # 🔑 Pass runtime configuration from your .env file
    env_file:
      - .env