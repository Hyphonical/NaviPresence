---
services:
  navicord:
    # 🖼️ Image published by GitHub Actions
    image: ghcr.io/hyphonical/navicord:latest
    # 🐳 Container name
    container_name: Navicord
    restart: unless-stopped
    # 🔑 Pass runtime configuration from your .env file
    env_file:
      - .env